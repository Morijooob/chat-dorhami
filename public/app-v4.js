const API='/api';let currentUser=loadUser(),socket=null,chatUser=null,currentRoom=null,authMode='login';
const $=id=>document.getElementById(id);
function loadUser(){try{return JSON.parse(localStorage.getItem('dorhami_user')||'null')}catch{return null}}
function saveUser(u){currentUser=u;localStorage.setItem('dorhami_user',JSON.stringify(u));window.me=u}
function clearUser(){currentUser=null;localStorage.removeItem('dorhami_user');window.me=null;if(socket){try{socket.close()}catch{}}socket=null}
function show(id,on){const x=$(id);if(x)x.classList.toggle('hidden',!on)}
function setError(t){const x=$('authError');if(x)x.textContent=t||''}
function esc(s){return String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}
function avatar(u){if(u?.avatar)return `<img src="${esc(u.avatar)}" alt="">`;return esc((u?.username||'?').trim().slice(0,1).toUpperCase())}
async function api(path,opt={}){const r=await fetch(API+path,{...opt,headers:{...(opt.body?{'Content-Type':'application/json'}:{}),...(opt.headers||{})}});let d={};try{d=await r.json()}catch{}if(!r.ok)throw Error(d.error||`خطای سرور (${r.status})`);return d}
async function hash(s){if(!crypto?.subtle)throw Error('مرورگر از رمزگذاری امن پشتیبانی نمی‌کند.');const b=await crypto.subtle.digest('SHA-256',new TextEncoder().encode(s));return [...new Uint8Array(b)].map(x=>x.toString(16).padStart(2,'0')).join('')}
function renderUser(){if(!currentUser)return;const n=$('me');if(n)n.textContent='@'+currentUser.username;const a=$('myAvatar');if(a)a.innerHTML=avatar(currentUser);const admin=$('adminNav');if(admin)admin.classList.toggle('hidden',currentUser.role!=='admin');const pn=$('profileName');if(pn)pn.textContent='@'+currentUser.username;const vip=$('vipBadge');if(vip)vip.textContent=currentUser.vip?'⭐ VIP':'';const pb=$('profileBig');if(pb)pb.innerHTML=avatar(currentUser)}
async function auth(e){e.preventDefault();setError('');const btn=e.submitter||document.querySelector('#authForm button[type="submit"]');if(btn)btn.disabled=true;try{const username=$('username')?.value.trim(),password=$('password')?.value||'';if(username.length<3)throw Error('نام کاربری باید حداقل ۳ کاراکتر باشد.');if(password.length<4)throw Error('رمز عبور حداقل ۴ کاراکتر باشد.');const d=await api(authMode==='register'?'/register':'/login',{method:'POST',body:JSON.stringify({username,passwordHash:await hash(password)})});if(!d.user)throw Error(d.error||'ورود انجام نشد.');saveUser(d.user);show('auth',false);show('chat',true);renderUser();await boot()}catch(err){setError(err.message||'عملیات انجام نشد.')}finally{if(btn)btn.disabled=false}}
function setMode(m){authMode=m;document.querySelectorAll('.tabs button').forEach(b=>b.classList.toggle('active',b.dataset.mode===m));const b=document.querySelector('#authForm button[type="submit"]');if(b)b.textContent=m==='login'?'ورود به دورهمی':'ساخت حساب و ورود';const p=$('password');if(p)p.autocomplete=m==='login'?'current-password':'new-password';setError('')}
async function boot(){if(!currentUser)return;try{const [rooms,users]=await Promise.all([api('/rooms?userId='+encodeURIComponent(currentUser.id)),api('/users')]);renderRooms(rooms.rooms||[]);renderUsers(users.users||[]);await recent()}catch(e){console.error(e)}}
function renderRooms(rs){const x=$('rooms');if(!x)return;x.innerHTML=rs.map(r=>`<button class="room-card" data-room="${esc(r.id)}"><div class="room-icon">💬</div><div class="room-info"><strong>${esc(r.name)}</strong><small>${r.is_public?'اتاق عمومی':'اتاق گفتگو'}</small></div><span class="room-arrow">‹</span></button>`).join('');x.querySelectorAll('[data-room]').forEach(b=>b.onclick=()=>openRoom(rs.find(r=>String(r.id)===b.dataset.room)))}
function renderUsers(us){const x=$('users');if(!x)return;x.innerHTML=us.filter(u=>u.id!==currentUser?.id).map(u=>`<button class="user-item" data-user="${esc(u.id)}"><div class="avatar">${avatar(u)}</div><div class="user-info"><b>${esc(u.username)}</b><small>${u.online?'آنلاین':'آفلاین'}</small></div></button>`).join('');x.querySelectorAll('[data-user]').forEach(b=>b.onclick=()=>privateChat(us.find(u=>String(u.id)===b.dataset.user)))}
async function recent(){try{const d=await api('/recent-chats?userId='+encodeURIComponent(currentUser.id));const x=$('history');if(!x)return;x.innerHTML=(d.chats||[]).map(c=>`<button class="history-item" data-user="${esc(c.other_id)}"><div class="avatar">${avatar({username:c.username,avatar:c.avatar})}</div><div class="history-info"><b>${esc(c.username||'کاربر')}</b><small>${esc(c.body||'')}</small></div></button>`).join('');const all=await api('/users');x.querySelectorAll('[data-user]').forEach(b=>b.onclick=()=>{const u=(all.users||[]).find(z=>String(z.id)===b.dataset.user);if(u)privateChat(u)})}catch(e){console.error(e)}}
function header(title,sub=''){const t=$('chatWith');if(t)t.textContent=title;const s=$('status');if(s)s.textContent=sub}
function render(messages){const x=$('messages');if(!x)return;x.innerHTML=(messages||[]).map(messageHtml).join('');x.scrollTop=x.scrollHeight}
function messageHtml(m){const mine=String(m.sender_id||m.senderId)===String(currentUser?.id);return `<div class="message ${mine?'mine':'theirs'}" data-id="${esc(m.id)}"><div class="messageSender">${esc(m.username||'')}</div><div class="messageBody">${esc(m.body||m.text||'')}<span class="tick">${mine?'✓':''}</span></div></div>`}
function append(m){const x=$('messages');if(!x)return;x.insertAdjacentHTML('beforeend',messageHtml(m));x.scrollTop=x.scrollHeight}
function setTick(id,state){const e=document.querySelector(`.message[data-id="${CSS.escape(String(id))}"] .tick`);if(e)e.textContent=state==='read'?'✓✓':'✓'}
function connect(target){if(socket){try{socket.close()}catch{}}const p=location.protocol==='https:'?'wss':'ws';socket=new WebSocket(`${p}://${location.host}/ws?userId=${encodeURIComponent(currentUser.id)}&username=${encodeURIComponent(currentUser.username)}&roomId=${encodeURIComponent(target)}`);socket.onmessage=e=>{try{const d=JSON.parse(e.data);if(d.type==='history')render(d.messages||[]);else if(d.type==='private_message'||d.type==='room_message')append(d.message);else if(d.type==='message_delivered'||d.type==='message_read')setTick(d.messageId,d.type==='message_read'?'read':'delivered');}catch(err){console.error(err)}};socket.onerror=()=>{const x=$('messages');if(x&&!x.children.length)x.innerHTML='<div class="chatError">اتصال زنده برقرار نشد.</div>'}}
async function openRoom(r){if(!r)return;chatUser=null;currentRoom=r;show('empty',false);show('conversation',true);header('# '+r.name,'اتاق گفتگو');try{const d=await api(`/room-messages?roomId=${encodeURIComponent(r.id)}&userId=${encodeURIComponent(currentUser.id)}`);render(d.messages||[]);connect(r.id)}catch(e){const x=$('messages');if(x)x.innerHTML=`<div class="chatError">${esc(e.message)}</div>`}}
async function privateChat(u){if(!u)return;chatUser=u;currentRoom=null;show('empty',false);show('conversation',true);header('@'+u.username,u.online?'آنلاین':'آفلاین');try{const d=await api(`/private-messages?userId=${encodeURIComponent(currentUser.id)}&otherId=${encodeURIComponent(u.id)}`);render(d.messages||[]);connect('private:'+u.id)}catch(e){const x=$('messages');if(x)x.innerHTML=`<div class="chatError">${esc(e.message)}</div>`}}
function sendMessage(e){e.preventDefault();const input=$('message'),body=input?.value.trim();if(!body||!socket||socket.readyState!==WebSocket.OPEN)return;if(chatUser)socket.send(JSON.stringify({type:'private_message',receiverId:chatUser.id,body}));else if(currentRoom)socket.send(JSON.stringify({type:'room_message',body}));input.value='';input.focus()}
function closeConversation(){show('conversation',false);show('empty',true);if(socket){try{socket.close()}catch{}}socket=null;chatUser=null;currentRoom=null}
function profile(){show('profileModal',true)}
async function createRoom(){if(!currentUser?.vip&&currentUser?.role!=='admin')return alert('ساخت اتاق گفتگو فقط برای کاربران VIP فعال است.');const name=prompt('نام اتاق را وارد کنید');if(!name)return;try{await api('/create-room',{method:'POST',body:JSON.stringify({creatorId:currentUser.id,name})});await boot()}catch(e){alert(e.message)}}
async function loadAdmin(){if(!currentUser||currentUser.role!=='admin')return;const x=$('adminPanel');if(!x)return;try{const [s,u]=await Promise.all([api('/admin/stats?userId='+encodeURIComponent(currentUser.id)),api('/admin/users?userId='+encodeURIComponent(currentUser.id))]);x.innerHTML=`<div class="admin-title">مدیریت دورهمی</div><div class="admin-stats"><div>کاربران<b>${s.users}</b></div><div>پیام‌ها<b>${s.messages}</b></div><div>اتاق‌ها<b>${s.rooms}</b></div></div><div class="admin-users">${(u.users||[]).map(a=>`<div class="admin-user"><b>${esc(a.username)}</b><span>${esc(a.role)}${a.vip?' · VIP':''}</span></div>`).join('')}</div>`}catch(e){x.textContent=e.message}}
function init(){const form=$('authForm');form?.addEventListener('submit',auth);document.querySelectorAll('.tabs button').forEach(b=>b.addEventListener('click',()=>setMode(b.dataset.mode)));$('sendForm')?.addEventListener('submit',sendMessage);$('backBtn')?.addEventListener('click',closeConversation);$('logout')?.addEventListener('click',()=>{clearUser();show('chat',false);show('auth',true);setMode('login')});$('profileBtn')?.addEventListener('click',profile);document.querySelectorAll('[data-close="profileModal"]').forEach(b=>b.addEventListener('click',()=>show('profileModal',false)));$('newRoom')?.addEventListener('click',createRoom);$('refreshHistory')?.addEventListener('click',recent);const search=$('search');search?.addEventListener('input',async()=>{try{const d=await api('/users?q='+encodeURIComponent(search.value.trim()));renderUsers(d.users||[])}catch{}});document.querySelectorAll('.nav-item').forEach(b=>b.addEventListener('click',()=>{document.querySelectorAll('.nav-item').forEach(x=>x.classList.remove('active'));b.classList.add('active');const p=b.dataset.panel;show('chatPanel',p==='chats');show('peoplePanel',p==='people');show('adminPanel',p==='admin');if(p==='admin')loadAdmin()}));setMode('login');if(currentUser){show('auth',false);show('chat',true);renderUser();boot()}else{show('auth',true);show('chat',false)}}
window.openRoom=openRoom;window.privateChat=privateChat;window.createRoom=createRoom;window.me=currentUser;
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init,{once:true});else init();